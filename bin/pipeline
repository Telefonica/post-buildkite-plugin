#!/usr/bin/env node

const plugin = require('../index');

const when = process.argv[2];
const steps = process.env;
try {
  const pipeline = plugin.getPipeline(when, steps);
  pipeline && console.log(pipeline);
} catch (err) {
  console.error(err.message);
  process.exit(1);
}
